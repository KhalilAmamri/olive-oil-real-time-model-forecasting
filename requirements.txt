# Base dependencies (Python 3.13 compatible)
streamlit>=1.36,<2
pandas>=2.2,<3
numpy>=2.1,<3
scikit-learn>=1.5,<2
plotly>=5.22,<6
joblib>=1.3,<2

# Optional: Prophet for time series
# Prophet may not have wheels for Python 3.13 on Windows yet.
# If you need Prophet, try installing separately:
#   pip install "prophet>=1.1.5" --no-build-isolation
# Or comment the line below back in to attempt during bulk install.
# prophet>=1.1.5
